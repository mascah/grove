# Working on tasks

- `tasks.py` is the whole tool; run it with `python3 tasks.py`.
- Grove records live under `grove/`; run the Grove CLI as `grove`.
- A proposal branch is `worktree-shape-SLUG`, in a linked worktree under
  `.claude/worktrees/`.
